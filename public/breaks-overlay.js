let breakSceneState=null,breakSceneName='',adKey='',lastAdTick=0;
function breakBackdrop(){return '<div class="motion-background"><div class="aurora"></div><div class="aurora second"></div><div class="horizon-grid"></div><div class="light-streak"></div></div>';}
function sponsorTile(p){return `<div class="partner-tile"><div class="partner-art"><img src="${esc(isMslPartner(p)?'/assets/msl-ph-black-wide.png':p.image)}" alt="${esc(p.name)}"></div><b>${esc(p.name)}</b>${p.tagline?`<small>${esc(p.tagline)}</small>`:''}</div>`;}
function renderBreakScene(scene,s){breakSceneName=scene;breakSceneState=s;adKey='';if(scene==='intermission'){stage.innerHTML=`<div class="fullscreen break-screen">${breakBackdrop()}${header()}<div class="intermission-main"><div class="eyebrow">INTERMISSION / STAY WITH US</div><h1>${esc(s.breaks.headline)}</h1><p>${esc(s.breaks.message)}</p><div class="return-clock"><span>BACK IN</span><b data-clock="breakTimer">${clock(s.breakTimer)}</b><i></i></div></div>${s.breaks.sponsors.length?`<div class="break-partners"><span>WITH THE SUPPORT OF</span><div>${s.breaks.sponsors.map(p=>`<div class="partner-mini"><img src="${esc(p.image)}" alt="${esc(p.name)}"><b>${esc(p.name)}</b></div>`).join('')}</div></div>`:''}${ticker()}</div>`;}else if(scene==='sponsors'){stage.innerHTML=`<div class="fullscreen break-screen">${breakBackdrop()}${header()}<div class="sponsor-show"><div class="eyebrow">THE PEOPLE BEHIND THE PLAY</div><h1>${esc(s.breaks.sponsorHeading)}</h1><div class="partner-grid ${s.breaks.sponsors.length>6?'many':''}">${s.breaks.sponsors.map(sponsorTile).join('')}</div>${!s.breaks.sponsors.length?'<p class="partner-empty">THANK YOU FOR SUPPORTING THE COMPETITION</p>':''}</div>${ticker()}</div>`;}else{stage.innerHTML=`<div class="fullscreen ads-screen"><div class="ad-media-host"></div><div class="ad-empty" ${s.breaks.ads.length?'hidden':''}><div class="eyebrow">${esc(s.event)}</div><h1>WE WILL BE RIGHT BACK</h1><p>The broadcast continues shortly.</p></div><div class="ad-bug"><span>ADVERTISEMENT</span><b>${esc(s.event)}</b></div><div class="ad-progress"><i></i></div><div class="ad-playback-error" hidden>Ad could not play. Check the file format or browser audio permission.</div></div>`;tickAds();}}
function tickAds(){if(breakSceneName!=='ads'||!document.querySelector('.ads-screen')||!breakSceneState)return;if(!breakSceneState.visible){document.querySelector('.ads-screen video')?.pause();return;}const b=breakSceneState.breaks,position=Breaks.currentAd(b),a=b.ads[position.index];if(!a)return;const key=JSON.stringify([position.index,a]);const host=document.querySelector('.ad-media-host');if(key!==adKey){adKey=key;host.innerHTML=a.type==='image'?`<img class="ad-media" style="object-fit:${a.fit}" src="${esc(a.src)}" alt="${esc(a.name)}">`:`<video class="ad-media" style="object-fit:${a.fit}" src="${esc(a.src)}" ${a.muted?'muted':''} autoplay playsinline loop></video>`;const media=host.firstElementChild;document.querySelector('.ad-playback-error').hidden=true;media.onerror=()=>document.querySelector('.ad-playback-error').hidden=false;if(a.type==='video'){media.muted=a.muted;media.onloadedmetadata=()=>{if(b.rotation.running&&Number.isFinite(media.duration)&&media.duration>0)media.currentTime=position.elapsed%media.duration;media.play().catch(()=>document.querySelector('.ad-playback-error').hidden=false);};}}document.querySelector('.ad-progress i').style.width=(b.rotation.running?position.elapsed/a.duration*100:0)+'%';}
setInterval(tickAds,200);
